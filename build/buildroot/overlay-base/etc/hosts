# Test convenience for the QEMU enrollment run: resolve the EST server without
# DNS (nsswitch is "hosts: files dns"). Production nodes resolve the EST server
# via real DNS (AAAA) instead of this static entry. Replace the placeholder
# address/hostname below with your EST server for a local enrollment run.
127.0.0.1	localhost
127.0.1.1	buildroot
::1		localhost ip6-localhost ip6-loopback
2001:db8::61	est.example.com
